{
  "$schema": "https://schema.railpack.com",

  "deploy": {
    "startCommand": "uvicorn main:api --host 0.0.0.0 --port 8080",

    "aptPackages": [
      "ffmpeg"
    ],

    "inputs": [
      "..."
    ]
  }
}